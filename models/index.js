const User = require('./user.model')
const Student = require('./student.model')
const NFT = require('./nft.model')
const Cart = require('./cart.model')

module.exports = {
    User, Student, NFT, Cart
}